import { HTTP } from 'meteor/http';
import { Meteor } from 'meteor/meteor';

import { Subscriptions } from '../lib/Subscriptions';

Subscriptions.after.insert(function (userId, doc) {

  
});
Subscriptions.after.update(function (userId, doc) {

});
Subscriptions.after.remove(function (userId, doc) {

});
